import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'speak.search.do.ifrn.marcilio',
  appName: 'Speak to search, Speak to do',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
